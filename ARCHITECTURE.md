# AIStock — 技术架构总览

> 版本：v0.4 | 更新日期：2026-04-27

---

## 一、整体架构

核心产品是一份 **10 维度 AI 研究报告**，由三层协同生成（⑦上下游 + ⑧同行对比已合并入 ⑥行业分析）：

```
┌─────────────────────────────────────────────────────────┐
│                    前端 / API 层                          │
│          (Web UI / REST API / CLI)                       │
│          流式输出，各维度完成后逐节推送                      │
└────────────────────────┬────────────────────────────────┘
                         │ 用户输入股票代码
                         ▼
┌─────────────────────────────────────────────────────────┐
│               第一层：信息采集层（并行）                    │
│                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │财务数据   │ │公告/新闻  │ │券商研报   │ │雪球/社区  │  │
│  │AKShare   │ │巨潮资讯   │ │东方财富   │ │公开内容   │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
│                    DataFetcherAgent                      │
└────────────────────────┬────────────────────────────────┘
                         │ 结构化信息包（按维度分类）
                         ▼
┌─────────────────────────────────────────────────────────┐
│               第二层：分析层（有序执行）                    │
│                                                         │
│  Step 1（先跑，定义边界）                                  │
│  ┌─────────────────┐  ┌─────────────────┐              │
│  │  主营业务 Agent  │  │  行业分析 Agent  │              │
│  │  确定公司做什么   │  │  确定同行范围    │              │
│  └─────────────────┘  └─────────────────┘              │
│                                                         │
│  Step 2（并行，依赖 Step 1 输出）                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐               │
│  │过往业绩   │ │同行对比   │ │股东分析   │               │
│  │  Agent   │ │  Agent   │ │  Agent   │               │
│  └──────────┘ └──────────┘ └──────────┘               │
│                                                         │
│  Step 3（并行，依赖 Step 1+2 输出）                        │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐        │
│  │盈利  │ │管理层│ │研报  │ │催化剂│ │上下游│        │
│  │预测  │ │情况  │ │聚合  │ │分析  │ │分析  │        │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘        │
│  ┌──────┐                                              │
│  │大牛  │                                              │
│  │分析  │                                              │
│  └──────┘                                              │
└────────────────────────┬────────────────────────────────┘
                         │ 11 个维度的结构化分析输出
                         ▼
┌─────────────────────────────────────────────────────────┐
│               第三层：综合叙事层                           │
│                  ReportSynthesizer                       │
│                                                         │
│  读取全部维度输出 → 生成统一结论                            │
│  • 综合投资判断（一段话）                                   │
│  • 核心支撑逻辑（3条）                                     │
│  • 主要风险（2条）                                        │
│  • 关键待观察指标                                          │
└─────────────────────────────────────────────────────────┘
```

---

## 二、10 个分析维度与对应 Agent

| # | 维度 | Agent | 主要数据源 | 执行顺序 |
|---|------|-------|----------|---------|
| ① | 主营业务 | BusinessAgent | 年报、研报 | Step 1 |
| ② | 过往业绩 | PerformanceAgent | AKShare 财务数据 | Step 2 |
| ③ | 盈利预测 | ForecastAgent | 研报预期 | Step 4 |
| ④ | 股东分析 | ShareholderAgent | 公告、AKShare | Step 2 |
| ⑤ | 管理层情况 | ManagementAgent | 年报、减持/质押公告 | Step 3 |
| ⑥ | 行业分析 | IndustryAgent | 行业数据 + 资金流 + 同行对比 | Step 1 |
| ⑦ | 最新催化剂 | CatalystAgent | 公告、新闻 | Step 3 |
| ⑧ | 研究报告 | ResearchAgent | 东财公开研报（PDF/文本） | Step 3 |
| ⑨ | 大牛分析 | KOLAgent | 雪球、公开文章 | Step 4 |
| 📋 | 综合结论 | ReportSynthesizer | 汇总以上 9 个维度 | 最后 |

> **合并说明**：⑦上下游情况（SupplyChainAgent）和 ⑧同行对比（PeerAgent）
> 已合并入 ⑥行业分析（IndustryAgent），包含产业链门槛 + 市场空间量化 + 同行对比表 + 行业资金流向。

> **信息分类原则**：一条信息可同时流入多个 Agent（例如大股东减持公告同时进入股东分析、管理层情况、最新催化剂三个维度）。

---

## 三、数据流详解

### 3.1 单股研究报告生成流

```
用户输入："600519"
    │
    ▼
DataFetcherAgent（并行采集，目标 <30s）
    ├── 财务数据（近5年年报、近8季度季报）
    ├── 公告（近1年，含减持/质押/业绩/重大事项）
    ├── 新闻（近3个月）
    ├── 东财研报列表 + PDF 内容
    └── 雪球公开内容（可选）
    │
    ▼（按维度分类，一条数据可进入多个维度）
    │
    ├─→ Step 1（并行）
    │       BusinessAgent → 主营业务摘要
    │       IndustryAgent → 产业链门槛 + 市场空间 + 同行对比表 + 行业资金流
    │
    ├─→ Step 2（并行，等待 Step 1 完成）
    │       PerformanceAgent → 历史财务趋势
    │       ShareholderAgent → 股东结构 + 变动
    │
    ├─→ Step 3（并行，等待 Step 2 完成）
    │       ManagementAgent  → 管理层背景 + 行为信号
    │       ResearchAgent    → 研报聚合（共识 + 分歧 + 目标价）
    │       CatalystAgent    → 近期催化剂梳理
    │
    ├─→ Step 4（并行，等待 Step 3 完成）
    │       ForecastAgent    → 盈利预测
    │       KOLAgent         → 大牛观点（可选，数据不足时跳过）
    │
    └─→ ReportSynthesizer
            读取全部输出 → 综合结论
```

### 3.2 流式输出节奏

```
t=0s   用户提交请求，显示加载状态
t=30s  数据采集完成
t=45s  主营业务、行业分析出现（Step 1 完成）
t=90s  过往业绩、同行对比、股东分析出现（Step 2 完成）
       → 每节顶部显示：「最终综合结论尚未生成，请以完整报告结论为主」
t=180s 其余维度逐步出现（Step 3 陆续完成）
t=240s 综合结论生成，提示消失，报告完整呈现
```

### 3.3 多股对比流

```
用户输入多个股票代码（2-4 支）
    → 并行生成各自的完整报告
    → 对比视图：抽取同行对比、管理层情况等关键维度横向展示
    → 辅助最终选择决策
```

---

## 四、项目目录结构

```
AIStock/
├── src/
│   ├── agents/
│   │   ├── data_fetcher.py          # 信息采集层（已实现）
│   │   ├── fundamental.py           # 原基本面Agent（将拆分复用）
│   │   ├── technical.py             # 暂缓，后续评估
│   │   │
│   │   │   # 11 维度 Agents（按 PRD 路线图逐步实现）
│   │   ├── business.py              # 主营业务
│   │   ├── industry.py              # 行业分析
│   │   ├── performance.py           # 过往业绩
│   │   ├── peer.py                  # 同行对比
│   │   ├── shareholder.py           # 股东分析
│   │   ├── forecast.py              # 盈利预测
│   │   ├── management.py            # 管理层情况
│   │   ├── research.py              # 研究报告聚合
│   │   ├── catalyst.py              # 最新催化剂
│   │   ├── supply_chain.py          # 上下游情况
│   │   ├── kol.py                   # 大牛分析
│   │   │
│   │   └── report_synthesizer.py    # 综合叙事层
│   │
│   ├── models/
│   │   ├── agent_message.py         # 统一消息结构
│   │   └── model_router.py          # 多模型路由（已实现）
│   ├── data/
│   │   ├── akshare_client.py        # AKShare封装（已实现）
│   │   └── report_parser.py         # 研报PDF解析（待实现）
│   └── utils/
│       ├── cache.py                 # 数据缓存（已实现）
│       └── logger.py
├── tests/
├── docs/agents/                     # 各Agent设计文档
├── PRD.md
├── ARCHITECTURE.md
└── CLAUDE.md
```

---

## 五、Agent 通信协议

所有 Agent 通过统一的 `AgentMessage` 结构通信：

```python
@dataclass
class AgentMessage:
    request_id: str       # 本次报告的唯一 ID
    source_agent: str     # 发送方
    target_agent: str     # 接收方（"synthesizer" 表示送入综合层）
    dimension: str        # 对应的报告维度（如 "peer_comparison"）
    payload: dict         # 分析结果
    data_sources: list    # 使用的数据源列表（支持可追溯）
    confidence: float     # 数据完整度 0-1（不足时降级展示）
    metadata: dict        # 时间戳、模型版本等
```

---

## 六、信息完整度处理原则

| 置信度 | 显示方式 |
|-------|---------|
| ≥ 0.8 | 正常展示 |
| 0.5–0.8 | 展示内容 + 标注「数据部分缺失，结论仅供参考」|
| < 0.5 | 展示已有数据 + 标注「数据不足，本节仅作参考」|
| 无数据 | 隐藏该维度，不显示空节 |

---

## 七、技术选型

| 层 | 技术 |
|----|------|
| LLM | Anthropic Claude（claude-sonnet-4-6 主力，claude-haiku 快速维度）|
| 数据源 A 股 | AKShare |
| 数据源港美股 | yfinance |
| 研报解析 | pdfplumber + LLM 提炼 |
| 数据缓存 | Redis / 本地文件（初期）|
| 后端 API | FastAPI + Server-Sent Events（流式输出）|
| 任务调度 | asyncio（Agent 并行）|
| 前端 | React + TailwindCSS（规划中）|
| 图表 | ECharts |

---

## 八、通用基础数据层（Common Data Layer）

> **设计原则**：凡多个维度都需要的数据，在报告生成开始时一次性拉取，存入 session 上下文，各 Agent 直接读取，避免重复调用。

### 通用数据清单

| 数据项 | 字段 | 接口 | 被哪些维度使用 |
|--------|------|------|--------------|
| **股票基础档案** | 股票简称、行业、板块、上市时间 | `ak.stock_individual_info_em()` | ①②③④⑤⑥⑦⑧⑨⑩⑪ 全部 |
| **总股本** | 总股本（股）、流通股本 | `ak.stock_individual_info_em()` | ①②③⑧（市值换算、PE计算、EPS→净利润） |
| **当前股价** | 最新收盘价 | `ak.stock_zh_a_spot_em()` 或 yfinance | ②③（PE计算、隐含估值） |
| **总市值** | 总市值（亿元） | `ak.stock_individual_info_em()` | ①②③⑧ |
| **所属行业/板块** | 行业名称、东财行业代码 | `ak.stock_individual_info_em()` | ①⑥⑦⑧（行业PE、同行筛选） |
| **员工人数** | 员工总数 | `ak.stock_individual_info_em()` | ① |

### 实现建议（待实现）

```python
# src/agents/data_fetcher.py 中新增
def fetch_common(stock_code: str, force_refresh: bool = False) -> dict:
    """
    一次性拉取所有维度共用的基础数据。
    返回 dict 包含：
      code, name, industry, sector, market_cap_yi, shares_total,
      shares_float, price_current, listed_date, employees
    缓存 TTL: 当日有效（股价用较短 TTL，其余信息当日缓存）
    """
```

各 Agent 在 `data_fetcher.fetch()` 时自动获得这些字段，无需单独请求。当前 `info` 已包含大部分这些字段，正式实现时整理字段命名并保证一致性即可。

---

## 九、各维度展示内容与数据规划

> 展示顺序：公司自身信息 → 行业 → 外部聚合分析

### ① 公司概况 `business`

| 展示内容 | 数据字段 | 接口 | 状态 |
|---------|---------|------|------|
| 档案栏：行业·市值·员工数·上市年份 | 股票简称/行业/总市值/上市时间 | `ak.stock_individual_info_em()` | ✅ |
| 概念标签（最多6个） | 概念板块名称 | 东财 push2 反向查找 | ✅ |
| 公司定位（LLM，1-2句粗体） | 主营业务简介 | `ak.stock_mainbz_ths()` | ✅ |
| 主营收入结构饼图（产品/地区） | 收入占比、毛利率 | `ak.stock_mainbz_ths()` | ✅ |
| 风险三格：客户集中/供应商/竞争壁垒 | LLM 综合推断 | — | ✅ |

---

### ② 过往业绩 `performance`

| 展示内容 | 数据字段 | 接口 | 状态 |
|---------|---------|------|------|
| KPI 卡片：营收·净利润·净利率·ROE·毛利率·EPS | 财务摘要近5年 | `ak.stock_financial_abstract_ths()` | ✅ |
| 营收+净利润双柱图（5年）+ 增速折线 | 同上 | 同上 | ✅ |
| 毛利率/净利率趋势折线 | 同上 | 同上 | ✅ |
| PE-TTM 历史折线 + 中位线 | 日线 PE-TTM | `ak.stock_zh_valuation_baidu()` | ✅ |
| PB 历史折线 + 中位线 | 日线 PB | `ak.stock_zh_valuation_baidu()` | ✅ |
| 经营现金流趋势（待补） | 经营活动净现金流 | `ak.stock_cash_flow_sheet_by_report_em()` | ❌ 待接入 |

---

### ③ 盈利预测 `forecast` ✅ 已实现

| 展示内容 | 数据字段 | 接口 | 状态 |
|---------|---------|------|------|
| 详细指标预测表（营收/净利润/ROE/EPS等9行，历史实际+未来预测均值） | 9项指标 × 6年列 | `ak.stock_profit_forecast_ths(indicator='业绩预测详表-详细指标预测')` | ✅ |
| 逐机构预测表：机构·研究员·日期·评级·各年EPS | orgSName/researcher/publishDate/emRatingName/predictEps | 东财 `reportapi.eastmoney.com/report/list` 直调（保留 `researcher` 字段） | ✅ |
| 一致预期 EPS/净利润 区间（min/avg/max） | 最小值/均值/最大值/预测机构数 | `ak.stock_profit_forecast_ths(indicator='预测年报每股收益/净利润')` | ✅ |
| LLM 分析结论 | 基于去重后数据 | Claude | ✅ |

**去重规则**：同一机构只保留最新一份（按 `institution` 去重）；无 EPS 数据的条目跳过。评级统计按机构数（不按研报数），"覆盖机构数 N 家"更准确。

> **实现细节**：`researcher` 字段通过直调东财原始 API 获取（AKShare `stock_research_report_em()` 内部丢弃该字段）；取 limit=60 原始条目去重，最终保留约 20 家机构。

---

### ④ 股东分析 `shareholder` 🚧 规划中

**核心展示逻辑**：
- 股东人数↓ = 筹码集中 = 强势信号；机构/北向减持 = 警惕信号
- 从"谁持有"和"在增还是减"两个维度判断市场参与者的态度

**模块一：股东人数趋势**（筹码集中度）

| 展示内容 | 数据字段 | 接口 | 状态 |
|---------|---------|------|------|
| 股东总人数趋势折线（最近6-8期） | 股东总人数/较上期变化/行业平均 | `ak.stock_zh_a_gdhs_detail_em(symbol)` | 待验证 |
| 人均流通股/人均持股金额 | 人均流通股(股)/人均持股金额(元) | 同上 | 待验证 |

**模块二：前十大股东**（持股结构）

| 展示内容 | 数据字段 | 接口 | 状态 |
|---------|---------|------|------|
| 前十大股东表（名称·持股量·占比·较上期变化·股份类型） | 股东名称/持有数量/占总股本/持股变化/股份类型 | `ak.stock_gdfx_top_10_em(symbol, date)` | 待验证 |

**模块三：机构持仓**（公募基金、QFII、社保等）

| 展示内容 | 数据字段 | 接口 | 状态 |
|---------|---------|------|------|
| 机构汇总（机构总数/合计持股/占流通股比/各类型分布） | 机构类型/持仓家数/持股数量/市值/持股变化/占比 | `ak.stock_report_fund_hold(symbol, date)` | 待验证 |
| 机构持仓趋势（近3期对比） | 占流通股比变化 | 同上多期对比 | 待验证 |

**模块四：北向资金**（沪深港通）

| 展示内容 | 数据字段 | 接口 | 状态 |
|---------|---------|------|------|
| 北向资金持仓趋势（增持/减持市值折线） | 增持数量/增持市值/增持幅度/持股数量/占流通股比 | `ak.stock_hsgt_individual_em(symbol)` | 待验证 |
| 机构明细表（各外资机构持仓） | 机构名称/增持数量/增持市值/持股市值/占比 | 同上 | 待验证 |

**LLM 分析结论**：整合以上4个模块，判断：
1. 筹码集中度趋势（股东人数变化方向）
2. 机构态度（公募/社保/北向整体加仓 or 减仓）
3. 综合股东结构信号（看多/中性/谨慎）

---

### ⑤ 管理层情况 `management`

> **范围原则**：聚焦可量化的客观事实（名单·行为·风险），不做主观战略判断。战略执行力评估归入 ⑥ 行业分析。

| 展示内容 | 数据字段 | 接口 | 状态 |
|---------|---------|------|------|
| 董事会/高管名单（姓名·职务·持股·年薪） | 姓名/职务/持股数量/年薪 | `ak.stock_board_member_em()` | ✅ 已接入 |
| 大股东/高管增减持（近2年，含量化影响度） | 变动日期/类型/数量/均价 | `ak.stock_shareholder_change_ths()` | ✅ 已接入，字段映射调试中 |
| 影响度量化 | 交易金额/占总市值比/占流通股比 | 计算字段（依赖 info 总市值/流通股本） | ✅ 已实现 |
| 股权质押（比例分级预警：>50%红/>30%橙） | 股东名称/质押数量/比例/截止日 | `ak.stock_gpzy_individual_pledge_ratio_detail_em()` | ✅ 已接入 |
| 高管背景标签（华为系/阿里系等） | 高管简历文本 | 年报 PDF 解析（待调研） | ❌ Sprint 10 |

---

### ⑥ 行业分析 `industry`

| 展示内容 | 数据字段 | 接口 | 状态 |
|---------|---------|------|------|
| 行业定位（LLM 分析） | 行业名称/板块 | `ak.stock_individual_info_em()` | ✅ |
| 行业景气：行业整体 PE/近期走势 | 行业板块 K 线 | `ak.stock_board_industry_hist_em()` | ❌ 待接入 |
| 行业资金流向 | 净流入/净流出 | `ak.stock_sector_fund_flow_rank_em()` | ❌ 待接入（低优先） |
| **管理层DNA × 战略匹配度** | 高管背景标签 + 主营战略方向 | board_members + business_overview（LLM推理） | ❌ 待实现（依赖 Sprint 10 高管背景标签） |

> **管理层DNA × 战略匹配度 设计备忘**
>
> 管理层背景只有放在行业战略背景下才有投资价值。例如："董事长来自华为电气"是事实陈述，结合"公司做 HVDC 战略转型且 HVDC 与充电模块技术同源"，才成为战略执行力背书。
>
> 实现思路：行业分析 LLM prompt 新增「管理层-战略匹配度」一节，输入 `business_overview`（主营/战略方向）+ `board_members`（高管职务 + Sprint 10 补全的背景标签），输出匹配度评级（高/中/低）及关键匹配点。

---

### ⑦ 上下游情况 `supply_chain`

| 展示内容 | 数据字段 | 接口 | 状态 |
|---------|---------|------|------|
| 产业链位置+议价权（LLM知识型） | 主营构成/毛利率趋势 | 复用 business_overview + financials | ✅ 基本够用 |

---

### ⑧ 同行对比 `peer`

| 展示内容 | 数据字段 | 接口 | 状态 |
|---------|---------|------|------|
| 同行指标对比表（PE/PB/ROE/毛利率/营收增速/市值，目标公司高亮） | 同行代码列表 + 市场快照 | IndustryAgent peer_codes + `ak.stock_zh_a_spot_em()` | ⚠️ 数据有，chart_data 未构建 |

---

### ⑨ 最新催化剂 `catalyst`

| 展示内容 | 数据字段 | 接口 | 状态 |
|---------|---------|------|------|
| 重大公告列表（日期·类型·标题） | 公告标题/类型/日期 | `ak.stock_notice_report_em()` | ✅（数据有，列表渲染待完善） |
| 正面/负面信号分类（LLM） | 个股新闻 | `ak.stock_news_em()` | ✅ |

---

### ⑩ 研究报告 `research`

| 展示内容 | 数据字段 | 接口 | 状态 |
|---------|---------|------|------|
| 分析师预测汇总表（机构·日期·评级·EPS） | 东财研报元数据 | `ak.stock_research_report_em()` | ✅ |
| 研报共识/分歧 LLM 分析 | 研报 PDF 正文 | 东财 PDF 链接 | ✅ |

---

### ⑪ 大牛分析 `kol`

| 展示内容 | 数据字段 | 接口 | 状态 |
|---------|---------|------|------|
| 各KOL投资风格匹配判断（LLM） | KOL 配置 + 公司基本面 | kol_config.py | ✅ |
| 公募持仓（低优先，复杂度高） | 基金重仓股 | `ak.fund_portfolio_hold_em()` | ❌ 待接入 |

---

## 九、已实现 vs 待实现

| 组件 | 状态 | 说明 |
|------|------|------|
| DataFetcherAgent | ✅ 已实现 | A股/港股数据采集，含缓存 |
| AKShare 客户端 | ✅ 已实现 | 财务/行情/估值/股东/新闻 |
| model_router | ✅ 已实现 | Claude / GPT / DeepSeek 多模型 |
| cache 工具 | ✅ 已实现 | Redis + 文件双层缓存 |
| FundamentalAgent | ✅ 已实现 | 将部分逻辑复用到 PerformanceAgent / ForecastAgent |
| TechnicalAgent | ⏸ 暂缓 | 已实现但暂不纳入主报告流程 |
| 11 维度 Agents | 🔲 待实现 | 按 Phase 1→4 逐步实现 |
| ReportSynthesizer | 🔲 待实现 | Phase 1 末期实现基础版 |
| report_parser | ✅ 已实现 | 支持东财自动拉取 + 用户 PDF 上传，持久化缓存 |
| 流式输出（SSE） | 🔲 待实现 | Phase 1 搭建框架 |
| KOLAgent | ⏸ 暂搁置 | 模块已实现，暂时返回空节，待产品方向确认后恢复 |

---

## 十、港股数据覆盖说明（已知限制）

> 集成测试（2026-04）验证结论，记录港股各维度的数据源覆盖情况。

| 维度 | 港股支持情况 | 根因 | 处理方式 |
|------|------------|------|---------|
| ① 公司概况 | ✅ 部分支持（conf≈0.7） | yfinance 可获取基本面，但字段比 A 股少 | LLM 基于可用字段生成，自动降级 |
| ② 过往业绩 | ✅ 正常（conf=1.0） | yfinance 财务报表覆盖良好 | — |
| ③ 盈利预测 | ✅ 基本支持（conf≈0.5） | 分析师预测数据有限 | 置信度略低，UI 已有提示 |
| ④ 股东结构 | ⚠️ 数据稀缺（conf≈0.4） | AKShare 股东变动接口仅覆盖 A 股 | 降级展示，无大股东增减持记录 |
| ⑤ 管理层 | ✅ 基本支持（conf≈0.5） | 高管数据通过公司简介+LLM推理获取 | — |
| ⑥ 行业分析 | ✅ 部分支持（conf≈0.6） | 同行对比仅限 A 股同行，港股同行数据缺失 | LLM 基于知识库补充全球同行 |
| ⑦ 催化剂 | ⚠️ 数据稀缺（conf≈0.4） | 东财公告接口（`stock_notice_report_em`）不支持港股 | 降级为仅 LLM 推理，无真实公告数据 |
| ⑧ 研究报告 | ❌ 无数据（conf=0.0） | 东财研报 API（`stock_research_report_em`）仅覆盖 A 股 | 降级提示"暂无研报，可手动上传 PDF" |
| 综合结论 | ✅ 正常（conf≈0.8） | 基于可用维度数据合成 | — |

### 改进方向（非紧急）

- **研报**：港股研报可接入彭博/万得港股研报接口（付费），或引导用户手动上传 PDF（已支持）
- **公告/催化剂**：港交所 HKEX 披露易有结构化公告 API，可免费接入补充港股公告数据
- **股东变动**：港交所权益披露数据（SFC）可作为替代数据源
- **当前策略**：优先保障 A 股体验，港股作为扩展市场，数据不足时 UI 明确提示而非空白
